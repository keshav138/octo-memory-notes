## Topics Grouped — Mock Test Coverage

**Group 1: Neural Network & Transformer Internals**

- Neural Networks, Activation Functions
- FFN in transformers
- Attention entropy difference across layers (layer 1 vs layer 11)
- CNN train/test gap + dropout effect (95%→72%→84%)

**Group 2: Embeddings & Retrieval Mechanics**

- Bi-Encoder vs Cross-Encoder
- Dense vector retriever vs BM25
- Sentence window retrieval implementation
- HyDE performance on time-sensitive questions
- Vector store lacking surrounding context — LlamaIndex fix strategy

**Group 3: RAG Evaluation & Failure Diagnosis**

- Retriever vs generator bottleneck (context precision/recall)
- RAG eval metrics: faithfulness, answer relevancy, BERTScore, ROUGE-L, RAGAS
- Why non-ROUGE-L metrics being high still ≠ prod-ready
- Legal doc RAG: faithfulness 0.6, context recall 0.9 — conclusion
- RAG zero overlap consequence
- ROUGE-L vs "humanization" score
- Prompt improvements + RAG

**Group 4: Agents & Agentic Pipelines**

- Agent episodic memory
- Agentic pipeline step removal — consequences
- Agentic loop failures/errors
- Multi-agent review chain — quality plateau over time
- AutoGen group chat (situational)
- Agent eval scoring high but reasoning flawed
- ReAct for code Q&A

**Group 5: LlamaIndex Specifics**

- `response.source_nodes` vs `response.response`
- "LlamaIndex for everything" — architectural flaw
- Sentence window retrieval (overlaps Group 2)

**Group 6: LangChain / LCEL**

- Model chaining + LCEL composition
- LCEL `.invoke()`/`.ainvoke()` on every runnable — why (not just async)
- LangChain workflow w/ checkpointing — resume from node 3 with modified state (prod incident)

**Group 7: Production Systems & Reliability**

- 429 rate limit at regular intervals — thundering herd problem
- Client-side vs server-side rate limiting — why client-side isn't redundant
- Customer callback → architectural pattern
- MCP over stdio — prod needs simultaneous connections, what changes

**Group 8: Multi-Provider / Tool-Use Abstraction**

- Claude vs OpenAI tool result return strategy (context block vs message-with-tool)
- Multi-provider abstraction layer design
- Claude extended thinking + temp 0.3 + budget_tokens 5000 — error scenario

**Group 9: Safety & Guardrails**

- Input/output guardrails for prompt injection
- Hallucinations (general)
- Gemini File API — generate_content error scenario

**Group 10: System Design (Applied)**

- Architecture for: doc search engine, chatbot, fraud alert summarizer
- Web scraping: WebBaseLoader vs BeautifulSoup

---

That's 10 groups. Let me know which order you want to tackle them in (or I'll default to this sequence), and we'll start pulling documentation/study material group by group.