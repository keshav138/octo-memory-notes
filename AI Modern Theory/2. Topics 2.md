
**A. Classical ML — Diagnosis/Behavior Questions**

- Given a confusion matrix / precision-recall tradeoff, which model or threshold change fixes it
- "Model has high variance vs high bias" symptom → which technique fixes which (regularization, more data, more features, ensemble)
- Random Forest vs single Decision Tree — why does RF reduce variance but not bias
- SVM: what happens when you use the wrong kernel, or C/gamma are misconfigured (underfit vs overfit symptom)
- KNN: performance degrades in high dimensions — why, and what's the fix (dimensionality reduction, better distance metric)
- Class imbalance scenario — model gets high accuracy but poor recall on minority class — why, and how Naive Bayes/SVM/RF each respond to it differently
- Feature scaling omitted — which of these algorithms breaks (SVM, KNN) vs which doesn't care (Decision Trees, RF)

**B. Bias-Variance / Regularization Scenarios**

- L1 vs L2 — given a scenario (many irrelevant features vs multicollinearity), which to pick and why
- "Adding more data didn't help test accuracy" — diagnose whether it's a bias problem (data won't fix it) vs variance problem (data will)

**C. Evaluation Metric Selection Scenarios** (mirrors the RAG-eval pattern from Group 3, but classical ML)

- Given imbalanced fraud/fraud-adjacent data (matches the JD's "fraud alert summarizer" hint) — why accuracy is misleading, precision vs recall vs F1 vs AUC-ROC tradeoff scenario
- Given a business cost asymmetry (false negative costlier than false positive, or vice versa) — which metric/threshold to optimize

**D. Neural Network Adjacent (beyond what Group 1 covered)**

- Vanishing/exploding gradients — given a deep network symptom, diagnose and fix (init strategy, batch norm, residual connections)
- Learning rate too high/low — symptom in loss curve, fix
- Overfitting fixes beyond dropout — early stopping, data augmentation, weight decay — scenario asking "which one applies here"

**E. LLM Eval — one gap in the mock's own theme**

- Given both classical NLP metrics (BLEU/ROUGE) and embedding-based metrics disagreeing — same reasoning pattern as the ROUGE-L vs faithfulness question in Group 3, but framed for translation/summarization rather than RAG

Since the JD calls out "at least one project training/fine-tuning an open-source LLM" as a plus, I'd also expect one scenario question on **fine-tuning failure modes** (catastrophic forgetting, overfitting on small fine-tune datasets, LoRA rank too low/high symptom) — that's the one LLM-classical-ML crossover area not in your 10 groups at all.

Want me to build out full scenario-question + answer sets for any of these (A–E), same depth as the earlier groups?