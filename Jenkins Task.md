1.  Create your first Jenkins job and understand the build console by manually triggering a build.
2. Write a jenkinsfile using declarative syntax with multiple named stages the
3. Connect a pipeline to a git repo and run a jenkinsfile stored in the repo