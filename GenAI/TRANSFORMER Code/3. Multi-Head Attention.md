![[Pasted image 20260719065001.png]]This is probably the hardest class in the entire Transformer. Once you understand this, the rest of the architecture is mostly assembling pieces together.

Let's go through it exactly as the tensor changes. I'll also spend extra time on the two parts you mentioned:

1. **Why do we transpose?**
    
2. **Why do we need `W_o` after concatenating the heads?**
    

---

# Step 0: Suppose we have

Let's assume

```text
Batch Size = 2

Sentence Length = 4

d_model = 8

Heads = 2
```

Then

```text
d_k = 8 / 2 = 4
```

So every token embedding has

```text
8 numbers
```

and each attention head gets

```text
4 numbers.
```

The input tensor is

```text
(batch, seq_len, d_model)

=

(2,4,8)
```

Visualize one sentence:

```text
Sentence

Word1

[8 values]

Word2

[8 values]

Word3

[8 values]

Word4

[8 values]
```

---

# Constructor

```python
self.w_q = nn.Linear(d_model, d_model)
```

Notice

```python
Linear(8,8)
```

It doesn't change the size.

Instead it learns a **different representation** of the embedding.

Similarly

```python
self.w_k
```

learns another representation.

And

```python
self.w_v
```

learns another one.

These correspond to the matrices

```text
WQ

WK

WV
```

from the paper.

---

# First step in forward()

```python
query = self.w_q(q)
```

Shape

```text
(2,4,8)

↓

(2,4,8)
```

Same shape.

Different values.

Exactly the same happens for

```text
Key

Value
```

---

# Splitting into heads

Now comes

```python
query.view(batch,
           seq_len,
           heads,
           d_k)
```

Originally

```text
(2,4,8)
```

becomes

```text
(2,4,2,4)
```

Nothing changed.

We're simply saying

Instead of

```text
8 numbers
```

think of them as

```text
Head1

[4]

Head2

[4]
```

Visual

```text
Word

[1 2 3 4 5 6 7 8]

↓

Head1

[1 2 3 4]

Head2

[5 6 7 8]
```

---

# Why transpose?

This is the part most people struggle with.

After

```python
view(...)
```

we have

```text
(batch,
 seq_len,
 heads,
 d_k)

=

(2,4,2,4)
```

Let's label each dimension.

```text
Batch

↓

Sentence

↓

Head

↓

Embedding inside head
```

So memory looks like

```text
Sentence

Word1

Head1

Head2

Word2

Head1

Head2

Word3

Head1

Head2
```

---

But attention wants to process

**one head at a time.**

Instead of

```text
Sentence

↓

Heads
```

we want

```text
Head

↓

Sentence
```

So we swap

```text
seq_len

and

heads
```

using

```python
transpose(1,2)
```

Result

```text
(batch,
 heads,
 seq_len,
 d_k)

=

(2,2,4,4)
```

Now the layout is

```text
Batch

↓

Head1

Word1

Word2

Word3

Word4

↓

Head2

Word1

Word2

Word3

Word4
```

Now every head has access to the **entire sentence**.

This is exactly what we want.

Each head independently computes attention over all words.

---

## Why can't we leave it as `(batch, seq_len, heads, d_k)`?

Because later we compute

```python
query @ key.transpose(...)
```

PyTorch performs matrix multiplication over the **last two dimensions** while treating the earlier dimensions as independent batches.

If we kept

```text
(batch, seq_len, heads, d_k)
```

PyTorch would interpret

```text
Sentence1

↓

Head1

Head2
```

instead of

```text
Head1

↓

Whole sentence
```

Each head would no longer be an independent attention computation.

So the transpose isn't arbitrary—it groups the tensor so that **each head becomes its own mini-batch** over the sentence.

---

# Computing attention

Now

```text
query

(batch,h,seq,d_k)
```

times

```python
key.transpose(-2,-1)
```

What does

```python
transpose(-2,-1)
```

do?

Suppose

```text
Key

(batch,h,4,4)
```

Inside one head

```text
4 words

↓

4 features
```

We need

```text
Query

(4 words × 4 features)

×

Key

(4 features × 4 words)
```

So we swap

```text
(seq_len,d_k)

↓

(d_k,seq_len)
```

Result

```text
(batch,h,d_k,seq_len)
```

Now matrix multiplication works

```text
(4×4)

@

(4×4)

↓

(4×4)
```

Each row

```text
Word i
```

gets compared with

```text
every word
```

Result

```text
(batch,h,seq_len,seq_len)
```

Example

```text
Head1

      W1 W2 W3 W4

W1

0.8

0.1

0.0

0.1

W2

0.2

0.5

0.2

0.1

...
```

This is the attention matrix.

---

# Multiply by Value

Next

```python
attention_scores @ value
```

Shape

```text
(batch,h,seq,seq)

@

(batch,h,seq,d_k)

↓

(batch,h,seq,d_k)
```

Each word now receives a weighted combination of the Value vectors from all words.

---

# Combine heads

Now we have

```text
(batch,h,seq,d_k)
```

Example

```text
(2,2,4,4)
```

We no longer want separate heads.

We want one embedding again.

So

```python
transpose(1,2)
```

returns

```text
(batch,seq,h,d_k)
```

Example

```text
(2,4,2,4)
```

Now

```python
view(...)
```

joins

```text
2 heads

×

4 features
```

back into

```text
8 features
```

Result

```text
(batch,seq,d_model)

=

(2,4,8)
```

Visual

```text
Head1

[4 numbers]

+

Head2

[4 numbers]

↓

[8 numbers]
```

---

# Now your biggest question:

## Why multiply by `W_o`?

At this point we've already concatenated the heads.

So why another Linear layer?

This is a very common question.

Imagine Head 1 learns:

```text
Grammar
```

and Head 2 learns:

```text
Word meaning
```

After concatenation we have

```text
[Grammar features | Meaning features]
```

They are simply stuck together.

Nothing has **combined** them yet.

`W_o` is the layer that lets the model **mix information across heads**.

Without `W_o`, the heads would remain isolated: head 1's outputs would always occupy the first `d_k` positions, head 2's outputs the next `d_k`, and so on.

Think of concatenation like placing reports from different specialists side by side:

```text
Doctor A report

Doctor B report

Doctor C report
```

Concatenation merely staples the reports together.

`W_o` is like an experienced consultant who reads **all** the reports and writes a single integrated summary.

Mathematically,

```text
Concatenated heads

↓

Linear(d_model → d_model)

↓

Mixed representation
```

Suppose

```text
Head1

[1 2 3 4]

Head2

[5 6 7 8]
```

Concatenation gives

```text
[1 2 3 4 5 6 7 8]
```

Without `W_o`, the model can only pass those values onward in that fixed arrangement.

With `W_o`, the output becomes something like

```text
[2.1, -0.7, 5.3, ...]
```

where **every output dimension can depend on every value from every head**. This allows the model to learn interactions between the different kinds of information each head discovered.

---

# The complete flow

```text
Input
(2,4,8)
        │
        ▼
WQ WK WV
        │
        ▼
(2,4,8)
        │
        ▼
Split into heads
        │
        ▼
(2,4,2,4)
        │
        ▼
Transpose
        │
        ▼
(2,2,4,4)
        │
        ▼
Attention
        │
        ▼
(2,2,4,4)
        │
        ▼
Transpose back
        │
        ▼
(2,4,2,4)
        │
        ▼
Concatenate heads
        │
        ▼
(2,4,8)
        │
        ▼
W_o
        │
        ▼
Final output
(2,4,8)
```

The two transposes serve different purposes:

- The **first transpose** rearranges the tensor so that each head can independently attend over the entire sequence.
    
- The **second transpose** undoes that rearrangement so the head outputs can be concatenated back into a single `d_model`-dimensional embedding before `W_o` mixes information across heads.